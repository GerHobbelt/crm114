TEST SCRIPT + COMMANDLINE:
-{ match <fromend> (:one_paragraph:) /[[:graph:]]+.*?\n\n/; learn [:one_paragraph:] < svm unique > (q_test.css) /[[:graph:]]+/; liaf } 

RETURN CODE:
0

STDERR TEST OUTPUT:

STDOUT TEST OUTPUT:

