TEST SCRIPT + COMMANDLINE:
-{ match <fromend> (:one_paragraph:) /[[:graph:]]+.*?:*:_nl::*:_nl:/; learn [:one_paragraph:] < svm unique > (i_test.css) /[[:graph:]]+/; liaf} 

RETURN CODE:
0

STDERR TEST OUTPUT:

STDOUT TEST OUTPUT:

